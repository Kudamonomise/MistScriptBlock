scripts:
  # 示例脚本组
  # 脚本组的名字
  example:
    # 脚本组的内容, 一个字符串列表
    # 使玩家以op身份执行命令 '/say hello, world!'
    - "@cmdop<-'say hello, world!'"
    - "@cmdcon<-'kick %player%'"

  goToSpawn:
    - "@cmd<-'spawn'"

  suicide:
    - "@cmd<-'suicide'"
